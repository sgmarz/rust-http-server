# Rust HTTP/HTTPS Server

This is a simple HTTP/HTTPS server that mimics the `http-server` from npm.

The NPM `http-server` was never used in behavior, source, or anything else to write this program.
